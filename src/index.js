import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// import axios from 'axios';
// 设置axios的
// axios.defaults.baseURL = process.env.near_analyze_backend;

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);